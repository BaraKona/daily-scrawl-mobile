import { createIconSet } from "@expo/vector-icons";

const glyphMap = require("../../../assets/tabler/glyph-map.json");
const TablerIcon = createIconSet(glyphMap, "tabler-icons", "tabler-icons.ttf");

export default TablerIcon;
